class Ventas
{
    int Venta;
    int Existencia;
    int Precio;
}

class Menus
{
    static void MenuPrincpial()
    {
        int  Menu = 0;

        while (Menu != 3)
        {
            Console.WriteLine("Menu Principal");
            Console.WriteLine("1) Inventaio");
            Console.WriteLine("2) Ventas");
            Console.WriteLine("3) Salir");
            Console.WriteLine();
            Console.WriteLine("Eliga una Opcion");
            Menu = int.Parse(Console.ReadLine());

            switch (Menu)
            {
                case 1:
                    MenuInventario();
                    break;

                case 2:
                    MenuVentas();
                    break;
                case 3:
                    Console.WriteLine("Estas Saliendo");
                    break;

                default:
                    Console.WriteLine("Opcion No Valida");
                    break;


            }
        }
    }
    

    static void MenuInventario()
    {

        char op = ' ', sn = ' ';
        int r = 0, clave = 0, ren = 0;
        bool existe = false;
        string c_b, nuevodato;
        string[,] datos = new string[100, 4];

        while (op != 'E' && op != 'e')
        {
            Console.Clear();
            Console.WriteLine(" I N V E N T A R I O ");
            Console.WriteLine(" A) Insertar datos ");
            Console.WriteLine(" B) Modificar datos ");
            Console.WriteLine(" C) Eliminar datos ");
            Console.WriteLine(" D) Listar datos ");
            Console.WriteLine(" E) Finalizar ");
            Console.WriteLine();
            Console.Write("Digita tu opcion: ");
            op = Console.ReadKey().KeyChar;
            switch (Char.ToUpper(op))
            {
                case 'A':
                    clave = r + 1;
                    datos[r, 0] = clave.ToString();
                    Console.WriteLine();
                    Console.Write("Telea nombre del producto: ");
                    datos[r, 1] = Console.ReadLine();
                    Console.WriteLine();
                    Console.Write("Telea Existencia del producto: ");
                    datos[r, 2] = Console.ReadLine();
                    Console.WriteLine();
                    Console.Write("Telea valor de venta del producto: ");
                    datos[r, 3] = Console.ReadLine();
                    r = r + 1;
                    break;
                case 'B':
                    Console.WriteLine();
                    Console.Write("Teclea la clave del producto: ");
                    c_b = Console.ReadLine();
                    for (int i = 0; i <= r; i++)
                    {
                        if (datos[i, 0] == c_b)
                        {
                            existe = true;
                            ren = i;
                        }
                    }
                    if (existe == true)
                    {
                        Console.WriteLine();

                        Console.WriteLine(" MODIFICACION DE DATOS");
                        Console.WriteLine("PRESIONA tecla <ENTER> SI NO MODICAS EL DATO");
                        Console.WriteLine($"Clave: {datos[ren, 0]}");
                        Console.WriteLine($"Producto: {datos[ren, 1]}");
                        Console.Write("Nuevo nombre producto : ");
                        nuevodato = Console.ReadLine();

                        if (nuevodato != "")

                            datos[ren, 1] = nuevodato;
                        Console.WriteLine($"Existencia: {datos[ren, 2]}");
                        nuevodato = Console.ReadLine();

                        if (nuevodato != "")

                            datos[ren, 2] = nuevodato;
                        Console.WriteLine($"Precio: {datos[ren, 3]}");
                        nuevodato = Console.ReadLine();

                        if (nuevodato != "")

                            datos[ren, 3] = nuevodato;
                        existe = false;
                        ren = 0;

                    }
                    else
                    {

                        Console.WriteLine("La clave tecleada no existe");
                        Console.ReadKey();
                    }
                    break;
                case 'C':
                    Console.WriteLine();
                    Console.Write("Teclea la clave del producto: ");
                    c_b = Console.ReadLine();
                    for (int i = 0; i <= r; i++)
                    {
                        if (datos[i, 0] == c_b)

                        {

                            existe = true;
                            ren = i;

                        }
                    }
                    if (existe == true)
                    {
                        Console.WriteLine();

                        Console.WriteLine(" ELIMINACION DE DATOS");

                        Console.WriteLine();

                        Console.WriteLine($"Clave: {datos[ren, 0]}");
                        Console.WriteLine($"Producto: {datos[ren, 1]}");
                        Console.WriteLine($"Existencia: {datos[ren, 2]}");
                        Console.WriteLine($"Precio: {datos[ren, 3]}");
                        Console.Write("Es el registro a das de baja(S/N): ");
                        sn = Console.ReadKey().KeyChar;

                        if (Char.ToUpper(sn) == 's' || Char.ToUpper(sn) == 'S')

                        {
                            for (int x = ren; x <= r; x++)
                            {
                                datos[x, 0] = datos[x + 1, 0];
                                datos[x, 1] = datos[x + 1, 1];
                                datos[x, 2] = datos[x + 1, 2];
                                datos[x, 3] = datos[x + 1, 3];

                            }

                            r = r - 1;

                        }
                        ren = 0;

                    }
                    else
                    {
                        Console.WriteLine("La clave tecleada no existe");
                        Console.ReadKey();

                    }
                    break;
                case 'D':
                    Console.WriteLine();
                    Console.WriteLine("LISTA DE PRODUCTOS");
                    for (int i = 0; i <= r; i++)
                    {
                        Console.WriteLine(); Console.WriteLine($"{datos[i, 0]}--{datos[i, 1]}--{datos[i, 2]}--{datos[i, 3]} ");
                    }
                    Console.ReadKey();
                    break;
                case 'E':
                    break;
                default:
                    break;
            }
        }
    }

    static List Ventas ()
    {
